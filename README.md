# MSPA
Medical Storage and Predictive Analysis

# Запуск DeepSeek тестов всегда через env-переменную:
DEEPSEEK_API_KEY=sk-xxx ./gradlew :application:test --tests "ai.DeepSeekRealFilesIntegrationTest"
